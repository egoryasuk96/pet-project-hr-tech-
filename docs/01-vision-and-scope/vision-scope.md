# Vision & Scope

**Продукт:** Employee Service  
**ID:** DOC-VISION  
**Версия:** 1.0  
**Статус:** Baseline v1.0  
**Связанные документы:** [Глоссарий](./glossary.md), [Use Cases](../02-requirements/use-cases.md), [Business Rules](../02-requirements/business-rules.md), [Backlog](../backlog.md)

---

## 1. Цель системы

Employee Service — веб-сервис подачи и согласования внутренних заявок сотрудников (отпуск, справки и т.п.).

Сотрудник выбирает тип заявки из каталога, заполняет форму, отправляет её на согласование и видит статус. Заявка проходит последовательные этапы согласования, на каждом согласующий принимает решение: согласовать, отклонить или вернуть на доработку.

---

## 2. Цель проекта

Показать на сквозном примере путь системного аналитика: от описания проблемы и требований через процессы, модель данных и архитектуру к API-контракту и работающему MVP.

Главный результат — аналитическая документация. Реализация (FastAPI + PostgreSQL, лёгкий веб-клиент из статических HTML-страниц с JavaScript) нужна, чтобы подтвердить, что спецификации реализуемы.

---

## 3. Проблема

Кадровые и административные вопросы решаются устно или перепиской по почте. Единого места, где видны заявка и её статус, нет. Из-за этого:

- руководитель может забыть о вопросе или затянуть решение;
- у решения нет письменного следа, а кадровая служба требует подтверждение;
- сотрудник не видит статус и вынужден переспрашивать;
- нет единого журнала решений по заявке.

Employee Service собирает заявку, маршрут согласования и решения в одном месте.

---

## 4. Заинтересованные стороны

| Сторона | Интерес |
| :--- | :--- |
| Сотрудник (инициатор) | Быстро подать заявку, видеть статус, доработать заявку после возврата |
| Согласующий (руководитель, кадровая служба и др.) | Иметь очередь заявок и принимать решения |
| Владелец процесса (вне системы в MVP) | Прозрачность и единообразие процесса |
| Автор проекта | Трассируемый комплект артефактов для портфолио |

Администратор процессов относится к будущему развитию (см. [Backlog](../backlog.md)).

---

## 5. Роли

| Роль | Код | Описание |
| :--- | :--- | :--- |
| Сотрудник | `employee` | Создаёт заявки из каталога, отслеживает свои заявки, дорабатывает возвращённые |
| Согласующий | `approver` | Обрабатывает заявки, назначенные на его этап |

Отдельные роли «Руководитель» и «HR» не выделяются: это пользователи с ролью `approver`, назначенные на нужные этапы маршрута. В Target у пользователя **одна** системная роль (`role_id`); совмещение employee+approver через M:N **не** используется ([ADR-ORG-01](../03-diagrams/architecture/adr-org-model.md), BR-16). Для демо seed создаются отдельные учётки под каждую роль.

**Auth (Target):** вход по логину/паролю, JWT ([ADR-AUTH-JWT-01](../03-diagrams/architecture/adr-jwt-core-api.md)).

**Legacy / Previous Baseline:** выбор демо-роли через заголовок без login (ADR-AUTH-DEMO-01) — не Target runtime.

---

## 6. Scope MVP

1. **Каталог заявок** — просмотр 2–3 предустановленных типов (например, отпуск, справка) с описанием и полями формы.
2. **Создание и отправка заявки** — заполнение формы, сохранение черновика, отправка на согласование.
3. **Мои заявки и карточка заявки** — список со статусами, детальный просмотр, комментарии, история решений и смены статусов.
4. **Согласование** — последовательный маршрут, очередь согласующего, решения approve / reject / return.
5. **Возврат на доработку** — статус `returned`, редактирование инициатором, повторная отправка с первого этапа live-маршрута.
6. **Отмена заявки** — только в статусах `draft` и `returned`.
7. **Live configuration** маршрута и переходов: `ApprovalRoute` / stages / assignments и `ProcessTransition` читаются в runtime; snapshot маршрута и версий значений **не** используется ([ADR-LIVE-CFG-01](../03-diagrams/architecture/adr-live-config.md)).
8. **Технический контур** — REST API, OpenAPI, PostgreSQL, лёгкий веб-клиент (макеты Penpot → static HTML/JS).

Целевая модель также включает оргструктуру (Company → Department → Employee → User), справочники Process / Status / Action, Admin-конфигурацию процессов и смену пароля — см. ADR-ORG-01, ADR-ACTION-01 (реализация — последующие этапы после документации).

---

## 7. Вне scope

- автоопределение руководителя по оргструктуре (поле `manager_employee_id` — только данные);
- параллельные этапы и условные ветвления маршрута;
- полноценный BPM-движок / workflow engine;
- **версионирование процессов** и **snapshot** конфигурации / маршрута / значений полей (`RouteInstance*`, `FieldValueVersion`);
- SSO / AD / LDAP, 2FA;
- email- и push-уведомления, файловые вложения;
- мультитенантность;
- HRIS, payroll, электронная подпись;
- мобильное приложение; event sourcing; Kafka/RabbitMQ; микросервисы.

**Осознанное ограничение live config:** изменение активной конфигурации процесса может затронуть незавершённые заявки ([ADR-LIVE-CFG-01](../03-diagrams/architecture/adr-live-config.md)).

## 7.1. Отложено (backlog)

Уже проработано в документации, но не входит в текущий MVP-срез UI: личный кабинет и профиль, административная панель (типы заявок, маршруты, справочники), административный журнал действий и реестр всех заявок. Auth (login/JWT) — Target runtime ([ADR-AUTH-JWT-01](../03-diagrams/architecture/adr-jwt-core-api.md)), не backlog. Подробности отложенных тем — в [Backlog](../backlog.md).

---

## 8. Ключевые сценарии

Детализация, идентификаторы и связанные правила приведены в [Use Cases](../02-requirements/use-cases.md).

| Сценарий | Актёр |
| :--- | :--- |
| Просмотреть каталог типов заявок | Сотрудник |
| Создать и отправить заявку | Сотрудник |
| Просмотреть свои заявки и карточку заявки | Сотрудник |
| Обработать заявку (согласовать, отклонить, вернуть) | Согласующий |
| Доработать возвращённую заявку и отправить повторно | Сотрудник |
| Отменить заявку в статусе `draft` или `returned` | Сотрудник |

---

## 9. Допущения

1. Система обслуживает одно предприятие.
2. Пользователи, роли, типы заявок, маршруты и назначения созданы начальными данными для демо.
3. Согласующие назначены явно: по роли или по конкретному пользователю.
4. На этапе с несколькими согласующими достаточно одного approve (first-approve wins).
5. Маршрут строго последовательный.
6. Маршрут и переходы — **live** (без snapshot на заявку).
7. Аналитическая документация равноправна коду в поставке проекта.
8. Язык артефактов — русский.

---

## 10. Ограничения

| Тип | Ограничение |
| :--- | :--- |
| Организационное | Одно предприятие |
| Процессное | Нет параллельных этапов и автомаршрутизации по оргструктуре |
| Безопасность | JWT Bearer (login/password); без SSO и 2FA; demo-header — Legacy (superseded) |
| Контент | Нет файловых вложений |
| Платформа | Веб (браузер); нативного клиента нет |
| Движок | Встроенный последовательный approval engine, не внешний BPM |
| Конфигурация | Live config: правки могут влиять на in-flight заявки (ADR-LIVE-CFG-01) |

---

## 11. Критерии успеха

1. **Документация:** есть Vision, требования, диаграммы процессов и состояний, ERD, OpenAPI-контракт, критерии приёмки; связи между ними прослеживаются по ID.
2. **Демо-сценарий:** сотрудник создаёт и отправляет заявку → согласующий согласует, отклоняет или возвращает → сотрудник видит статус и при возврате дорабатывает и отправляет повторно.
3. **Маршрут:** для типа заявки задан последовательный маршрут; на этапе с несколькими согласующими срабатывает first-approve wins.
4. **Live config:** маршрут и transitions читаются из актуальной конфигурации; versioning/snapshot — out of scope.
5. **Отмена:** возможна только для `draft` и `returned`.
6. **Технологии:** API соответствует OpenAPI; сервис работает локально и в облаке (Render + Neon).
7. **Портфолио:** по документам можно проследить путь от проблемы до реализации без пояснений автора.

---

## 12. Зафиксированные решения

1. First-approve wins на этапе с несколькими согласующими.
2. Возврат на доработку: `returned` → редактирование → повторная отправка с первого этапа live-маршрута.
3. Отмена инициатором только для `draft` и `returned`.
4. Назначения согласующих явные, без автоопределения по оргструктуре.
5. Маршрут последовательный; **live config** (без snapshot) — [ADR-LIVE-CFG-01](../03-diagrams/architecture/adr-live-config.md).
6. Auth: JWT ([ADR-AUTH-JWT-01](../03-diagrams/architecture/adr-jwt-core-api.md)); demo-header superseded.
7. Интерфейс: Penpot-макеты → static HTML/JS (отдаёт FastAPI), без React/SPA.
8. Целевая модель: оргструктура, ProcessTransition, Admin config, смена пароля — в документации; реализация — последующие этапы.

---

## История изменений

| Версия | Дата | Описание |
| :--- | :--- | :--- |
| 0.1 | 2026-09-18 | Первая версия (Draft) |
| 1.0 | 2026-09-20 | Baseline: MVP сужен до ядра; интерфейс, роль и отложенные функции зафиксированы |
| 1.1 | 2026-09-23 | E0: отказ от snapshot; live config; out of scope versioning/snapshot |
