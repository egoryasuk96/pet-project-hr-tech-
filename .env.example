# Application
APP_NAME=Employee Service
APP_ENV=local

# PostgreSQL (Neon or local). Do not commit real secrets.
# Format: postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/employee_service
